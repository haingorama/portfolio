var scene = document.getElementById('scene');
				var parallax = new Parallax(scene);

// FORMATION
lightbox.option({
resizeDuration: 300,
wrapAround: true,
disableScrolling: true,
albumLabel: "%1 / %2",
});
